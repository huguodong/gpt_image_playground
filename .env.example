# 应用自有的公开链接和本地存储命名空间配置。
# 可复制为 `.env.local` 使用，或直接填写到部署平台的环境变量面板中。
# 修改任意 VITE_* 配置后，需要重启开发服务器或重新构建应用。

# 公开站点地址，会用于：
# - 顶部标题链接
# - 帮助弹窗底部链接
# 默认值： https://ai.52moyu.net
VITE_PUBLIC_SITE_URL=https://ai.52moyu.net

# 顶部“获取 API Key”按钮的独立跳转地址。
# 留空时会自动回退到 VITE_PUBLIC_SITE_URL。
# 示例：
# VITE_API_KEY_APPLY_URL=https://example.com/get-key
VITE_API_KEY_APPLY_URL=

# 统一品牌命名空间，会同时用于：
# - IndexedDB 数据库名
# - 持久化 UI 状态的存储 key
# - 导出 ZIP 文件名前缀
#
# 重要说明：
# 修改这个值后，不会自动继承旧命名空间下的浏览器本地数据。
# 如果需要保留原有本地数据，建议先导出 ZIP，
# 再切换命名空间，最后重新导入 ZIP。
VITE_APP_NAMESPACE=ai-52moyu-net
